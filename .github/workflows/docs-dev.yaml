# This file is @generated by <https://github.com/liblaf/copier-python>.
# DO NOT EDIT!

name: Docs (dev)

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  docs:
    name: Docs
    permissions:
      contents: write
      pages: write
    runs-on: ubuntu-latest
    concurrency:
      group: docs
    steps:
      - id: auth
        name: Auth App
        uses: liblaf/actions/auth-app@main
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ steps.auth.outputs.token }}
          fetch-depth: 0
      - name: Fetch `gh-pages`
        # ref: <https://github.com/jimporter/mike?tab=readme-ov-file#deploying-via-ci>
        run: git fetch origin gh-pages --depth=1
      - name: Setup Python
        uses: liblaf/actions/setup-python@main
      - if: hashFiles('docs/scripts/prepare.sh') != ''
        name: Prepare
        run: docs/scripts/prepare.sh
      - name: Deploy
        run: mike deploy --push dev
      - name: Set Default
        run: mike set-default --push dev
      - name: Setup GitHub Pages
        uses: liblaf/actions/setup-pages@main
        with:
          token: ${{ secrets.GH_PAT || steps.auth.outputs.token }}
